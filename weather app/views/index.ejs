<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../public/index.css">
    <title>Document</title>
</head>

<body>
    <h1>Node Js Weather App</h1>
    <form action="/weather" method="get">
        <input type="text" placeholder="Enter City" name="city">
        <button class="btn">Get Weather</button>
    </form>
    <%if(error){%>
        <p class="error">
            <%= error%>
        </p>
        <%}else{%>
            <div class="weather-container">
                <p>
                    📍 <%= weather.sys.country%>,
                        <%= weather.name%>
                </p>
                <p>
                    🌡️
                    <%= weather.main.temp%>&deg;F,
                        <%= ((weather.main.temp - 32 )/1.8).toFixed(2)%>&deg;C
                </p>
                <p>
                    👁️
                    <%= weather.visibility%>
                </p>
                <p>
                    🌬️
                    <%= weather.wind.speed%> m/h
                </p>
            </div>
            <%}%>
</body>

</html>